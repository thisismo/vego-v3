product: jvm/lib

apply:
  - ../../build-utils/server.module-template.yaml

dependencies:
  - ../../common/server-auth
  - $ktor.server.auth
  - $ktor.client.core
  - $ktor.client.cio
  - $ktor.client.contentNegotiation
  - ../identity-common